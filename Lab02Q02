Lab02Q02

As variáveis arg1 e arg2 não são alteradas pois as funções não alteram elas, apenas usam seus valores para funcionarem. As funções usadas no gdb foram, respectivamente: gdb prog (abre o programa no gdb); run (roda o programa); break (durante a execução, para nas linhas escolhidas) na linha 30, 31, 32, 33, print(imprime a variável) arg1 e print arg2.
